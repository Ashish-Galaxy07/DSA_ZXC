class Solution {
    public boolean uniformArray(int[] nums1) {
        //we can always create another array nums2 where 
        //all elements are either odd or all are even using the given method

        return true;
    }
}